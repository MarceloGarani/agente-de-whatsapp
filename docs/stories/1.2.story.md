# Story 1.2: WhatsApp Webhook & Client

## Status

Done

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["typecheck", "test:unit"]

## Story

**As a** user,
**I want** the system to receive my WhatsApp messages and be able to respond,
**so that** I can communicate with the assistant through my preferred messaging app.

## Acceptance Criteria

1. GET `/webhook` verifica `hub.verify_token` e retorna `hub.challenge` (Meta webhook verification)
2. POST `/webhook` responde HTTP 200 imediatamente e processa mensagem de forma assincrona (NFR-1)
3. POST `/webhook` extrai mensagem de `req.body.entry[0].changes[0].value.messages[0]` corretamente
4. `whatsapp.client.ts` implementa `sendText(to, body)` via POST graph.facebook.com/v21.0/{PHONE_ID}/messages
5. `whatsapp.client.ts` implementa `downloadMedia(mediaId)` com fluxo 2-step (GET URL → GET binary)
6. `whatsapp.client.ts` implementa `sendProcessingMessage(to)` que envia "Processando..." em < 2s (NFR-1)
7. Timeout de 10s em todas as chamadas ao WhatsApp Cloud API
8. Erros de API sao logados via pino e usuario notificado com mensagem descritiva (NFR-3)

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [x] **Task 1: Webhook handler** (AC: 1, 2, 3)
  - [x] Criar `src/webhooks/whatsapp.handler.ts`
  - [x] Implementar GET /webhook: verificar `hub.mode === 'subscribe'` e `hub.verify_token === env.WEBHOOK_VERIFY_TOKEN`, retornar `hub.challenge` com status 200, ou 403 se invalido
  - [x] Implementar POST /webhook: responder 200 IMEDIATAMENTE (res.sendStatus(200)), depois processar async com try/catch
  - [x] Extrair mensagem de `req.body.entry[0].changes[0].value.messages[0]` com validacao de payload structure
  - [x] Registrar rotas no `src/server.ts`
- [x] **Task 2: WhatsApp client** (AC: 4, 5, 6, 7)
  - [x] Criar `src/services/whatsapp.client.ts`
  - [x] Implementar `sendText(to: string, body: string)`: POST para `https://graph.facebook.com/v21.0/${env.WHATSAPP_PHONE_NUMBER_ID}/messages` com headers Authorization Bearer, Content-Type application/json, body: { messaging_product: 'whatsapp', to, type: 'text', text: { body } }
  - [x] Implementar `downloadMedia(mediaId: string)`: Step 1 — GET `https://graph.facebook.com/v21.0/${mediaId}` para obter URL. Step 2 — GET na URL retornada com responseType 'arraybuffer' para obter Buffer binario
  - [x] Implementar `sendProcessingMessage(to: string)`: chamar sendText com `messages.processing.default`
  - [x] Configurar axios com timeout de 10_000ms (TIMEOUTS.WHATSAPP_API) em todas as chamadas
- [x] **Task 3: Error handling** (AC: 8)
  - [x] Usar handleApiError() em todas as chamadas axios, logando via logger com service='whatsapp'
  - [x] Em caso de erro no POST /webhook, enviar mensagem de erro ao usuario via sendText (se possivel) usando messages.errors.generic
- [x] **Task 4: Testes unitarios**
  - [x] Criar `tests/unit/whatsapp.handler.test.ts`: testar GET verify (token valido/invalido), POST payload extraction
  - [x] Criar `tests/unit/whatsapp.client.test.ts`: testar sendText (mock axios), downloadMedia (mock 2-step), timeout handling

## Dev Notes

### Architecture Context [Source: docs/architecture.md]

**WhatsApp Cloud API v21.0 (Sec 5.1):**
- Base URL: `https://graph.facebook.com/v21.0/`
- Auth: Bearer token (WHATSAPP_ACCESS_TOKEN)
- Send message endpoint: `POST /{PHONE_NUMBER_ID}/messages`
- Media download: 2-step — GET `/{MEDIA_ID}` para URL, GET URL para binary
- Rate limit: Tier 1 = 250 conversas/24h
- Timeout: 10s (TIMEOUTS.WHATSAPP_API)

**Async Webhook Pattern (Sec 2.5, 6.1):**
```typescript
// POST /webhook — responder 200 IMEDIATAMENTE
app.post('/webhook', (req, res) => {
  res.sendStatus(200); // CRITICO: responder antes de processar
  processMessageAsync(req.body).catch(err => {
    logger.error({ service: 'webhook', action: 'process', error: err });
  });
});
```

**Webhook Verification (Sec 6.1):**
```typescript
// GET /webhook — Meta verification
app.get('/webhook', (req, res) => {
  const mode = req.query['hub.mode'];
  const token = req.query['hub.verify_token'];
  const challenge = req.query['hub.challenge'];
  if (mode === 'subscribe' && token === env.WEBHOOK_VERIFY_TOKEN) {
    res.status(200).send(challenge);
  } else {
    res.sendStatus(403);
  }
});
```

**Messages pt-BR (Sec 14.2):** Usar `messages.processing.default` ("Processando sua mensagem...") para sendProcessingMessage.

**Timeouts (Sec 11.5):**
```typescript
export const TIMEOUTS = {
  WHATSAPP_API: 10_000,
  OPENAI_GPT: 10_000,
  OPENAI_WHISPER: 15_000,
  GOOGLE_APIS: 10_000,
  NODEMAILER: 10_000,
} as const;
```

**Coding Standards (Sec 13):** Named exports, funcoes async/await, sem classes. Usar axios para REST calls.

**Project Structure:**
```
src/
├── webhooks/
│   └── whatsapp.handler.ts    # CRIAR
├── services/
│   └── whatsapp.client.ts     # CRIAR
```

**Dependencia de Story 1.1:** Requer env.ts, logger.ts, error-handler.ts, messages.ts, types/index.ts ja implementados.

### Testing

**Framework:** Jest + ts-jest
**Location:** tests/unit/whatsapp.handler.test.ts, tests/unit/whatsapp.client.test.ts
**Mocks:** Mock axios para chamadas HTTP. Mock env para tokens.
**Pattern:** Testar happy path + erro path para cada funcao exportada.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-16 | 0.1 | Story criada a partir do PRD Epic 1 + Architecture | @sm (River) |
| 2026-02-16 | 0.2 | Validacao GO (8/10) — Status Draft → Ready | @po (Pax) |
| 2026-02-16 | 0.3 | Implementacao completa — Status Ready → InReview. 4 tasks, 12 testes, 0 erros TS | @dev (Dex) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References
- Jest fix: moduleNameMapper para NodeNext `.js` extensions
- Jest fix: jest.mock() hoisted antes do import no handler test
- ts-jest warning fix: diagnostics.ignoreCodes [151002] para NodeNext

### Completion Notes List
- `src/webhooks/whatsapp.handler.ts` — Router com GET /webhook (verify) e POST /webhook (async process), extractMessage() com suporte a text/audio/document/image/unknown
- `src/services/whatsapp.client.ts` — sendText, downloadMedia (2-step), sendProcessingMessage usando axios com timeout 10s
- `src/server.ts` — Atualizado com webhookRouter
- 12 testes unitarios passando (7 handler + 5 client)
- TypeScript: zero erros (npx tsc --noEmit)

### File List
- `src/webhooks/whatsapp.handler.ts` (CREATED)
- `src/services/whatsapp.client.ts` (CREATED)
- `src/server.ts` (MODIFIED — added webhookRouter import/use)
- `tests/unit/whatsapp.handler.test.ts` (CREATED)
- `tests/unit/whatsapp.client.test.ts` (CREATED)
- `jest.config.ts` (MODIFIED — added moduleNameMapper + diagnostics)

## QA Results

**Verdict: PASS**

| Check | Result | Notes |
|-------|--------|-------|
| Code review | OK | WhatsApp client e webhook handler bem estruturados |
| Unit tests | OK | Cobertura adequada (handler + client) |
| Acceptance criteria | OK | Todos os ACs atendidos |
| No regressions | OK | Nenhuma regressao |
| Performance | OK | N/A |
| Security | OK | Webhook verify token validado |
| Documentation | OK | Dev Notes completas |

**TypeScript:** 0 erros | **Testes:** 108/108 passando, 12 suites

— Quinn, guardiao da qualidade
