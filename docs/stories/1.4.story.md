# Story 1.4: Message Router & End-to-End Text Flow

## Status

Done

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["typecheck", "test:unit", "test:integration"]

## Story

**As a** user,
**I want** to send a text message and receive an appropriate response based on my intent,
**so that** the assistant acts on my requests without me needing to learn commands.

## Acceptance Criteria

1. `message-router.ts` roteia por tipo: text → intent-classifier, audio → (futuro), document → intent `upload_arquivo`
2. Apos classificacao, intent router direciona para servico correspondente via `switch(intent)`
3. Para intents ainda nao implementados (drive, calendar), responde com "Funcionalidade em desenvolvimento"
4. Para intent `ajuda`, responde com lista de capacidades do assistente em pt-BR
5. Para intent `clarificacao`, responde pedindo mais detalhes com opcoes sugeridas
6. Webhook handler integrado com message router: mensagem recebida → classificada → respondida
7. Testes unitarios para roteamento por tipo e por intent
8. Teste de integracao: POST /webhook com mensagem texto → resposta correta via WhatsApp

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [x] **Task 1: Message router** (AC: 1, 2, 3, 4, 5)
  - [x] Criar `src/services/message-router.ts`
  - [x] Implementar `routeMessage(message: IncomingMessage): Promise<void>` que:
    - Envia sendProcessingMessage(message.from) imediatamente
    - Roteia por message.type: TEXT → classifyIntent(text), AUDIO → placeholder "audio em desenvolvimento", DOCUMENT → intent UPLOAD_ARQUIVO direto
    - Apos classificacao, switch(intent): UPLOAD_ARQUIVO → (futuro), CRIAR_EVENTO → (futuro), CRIAR_REUNIAO → (futuro), CANCELAR_EVENTO → (futuro), AJUDA → sendText com messages.prompts.help, CLARIFICACAO → sendText com messages.prompts.clarification
    - Para intents com servicos nao implementados: sendText com "Funcionalidade em desenvolvimento. Em breve!"
  - [x] Logging com service='message-router', action='route', intent=result.intent
- [x] **Task 2: Integrar webhook com router** (AC: 6)
  - [x] Atualizar POST /webhook em whatsapp.handler.ts: apos extrair mensagem, construir IncomingMessage e chamar routeMessage()
  - [x] Garantir que processamento e async (apos res.sendStatus(200))
- [x] **Task 3: Testes unitarios** (AC: 7)
  - [x] Criar `tests/unit/message-router.test.ts`
  - [x] Mock de intent-classifier e whatsapp.client
  - [x] Test: mensagem tipo TEXT → chama classifyIntent
  - [x] Test: mensagem tipo DOCUMENT → intent UPLOAD_ARQUIVO direto
  - [x] Test: intent AJUDA → envia messages.prompts.help
  - [x] Test: intent CLARIFICACAO → envia messages.prompts.clarification
  - [x] Test: intent sem servico implementado → envia "em desenvolvimento"
- [x] **Task 4: Teste de integracao** (AC: 8)
  - [x] Criar `tests/integration/webhook.test.ts`
  - [x] Usar supertest com app exportado de server.ts
  - [x] Test: GET /webhook com token valido → retorna challenge
  - [x] Test: GET /webhook com token invalido → retorna 403
  - [x] Test: POST /webhook com mensagem "ajuda" → status 200 (async processing)

## Dev Notes

### Architecture Context [Source: docs/architecture.md]

**Message Router Pattern (Sec 6.1, 7):**
- Roteia por tipo de mensagem (text/audio/document)
- Apos classificacao de intent, direciona para servico via switch
- Envia "Processando..." imediatamente antes de qualquer processamento

**Fluxo de Texto (Sec 6.1):**
```
POST /webhook → 200 OK → "Processando..." → classifyIntent(text) → switch(intent) → servico → confirmacao
```

**Messages pt-BR (Sec 14.2):**
- `messages.prompts.help`: lista de capacidades completa
- `messages.prompts.clarification`: opcoes sugeridas para mensagem ambigua

**Integration Test Pattern (Sec 12.3):**
```typescript
import request from 'supertest';
import { app } from '../../src/server';

describe('Webhook Integration', () => {
  it('GET /webhook verifica token', async () => {
    const res = await request(app).get('/webhook').query({
      'hub.mode': 'subscribe',
      'hub.verify_token': process.env.WEBHOOK_VERIFY_TOKEN,
      'hub.challenge': 'challenge123',
    });
    expect(res.status).toBe(200);
    expect(res.text).toBe('challenge123');
  });
});
```

**Project Structure:**
```
src/services/message-router.ts          # CRIAR
tests/unit/message-router.test.ts       # CRIAR
tests/integration/webhook.test.ts       # CRIAR
```

**Dependencia de Stories 1.1, 1.2, 1.3:** Requer whatsapp.handler.ts, whatsapp.client.ts, intent-classifier.ts, types, logger, error-handler, messages.

### Testing

**Unit:** Jest + ts-jest, mocks de intent-classifier e whatsapp.client
**Integration:** supertest com app importado de server.ts
**Nota:** Testes de integracao verificam status HTTP (200/403) mas NAO verificam conteudo do WhatsApp (chamadas async). Para verificar resposta enviada, mock do whatsapp.client no integration test.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-16 | 0.1 | Story criada a partir do PRD Epic 1 + Architecture | @sm (River) |
| 2026-02-16 | 0.2 | Validacao GO (8/10) — Status Draft → Ready | @po (Pax) |
| 2026-02-16 | 0.3 | Implementacao completa — Status Ready → InReview. 4 tasks, 13 testes novos, 0 erros TS | @dev (Dex) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References
- server.ts refatorado: app.listen protegido com `if (NODE_ENV !== 'test')` para permitir supertest sem conflito de porta
- whatsapp.handler.test.ts atualizado: adicionado mock de message-router (nova dependencia do handler)

### Completion Notes List
- `src/services/message-router.ts` — routeMessage() com roteamento por tipo (TEXT→classifyIntent, AUDIO→placeholder, DOCUMENT/IMAGE→UPLOAD_ARQUIVO) e por intent (AJUDA→help, CLARIFICACAO→clarification, outros→"em desenvolvimento")
- `src/webhooks/whatsapp.handler.ts` — Placeholder removido, integrado com routeMessage()
- `src/server.ts` — app.listen condicional para suporte a testes de integracao com supertest
- 7 testes unitarios message-router + 6 testes integracao webhook (incluindo /health)
- TypeScript: zero erros, 33/33 testes passando (5 suites)

### File List
- `src/services/message-router.ts` (CREATED)
- `src/webhooks/whatsapp.handler.ts` (MODIFIED — integrado com routeMessage)
- `src/server.ts` (MODIFIED — app.listen condicional)
- `tests/unit/message-router.test.ts` (CREATED)
- `tests/unit/whatsapp.handler.test.ts` (MODIFIED — mock de message-router)
- `tests/integration/webhook.test.ts` (CREATED)

## QA Results

**Verdict: PASS**

| Check | Result | Notes |
|-------|--------|-------|
| Code review | OK | Message router com fluxo claro e extensivel |
| Unit tests | OK | Cobertura ampla com edge cases |
| Acceptance criteria | OK | Todos os ACs atendidos |
| No regressions | OK | Nenhuma regressao |
| Performance | OK | N/A |
| Security | OK | Sem vulnerabilidades detectadas |
| Documentation | OK | Dev Notes completas |

**TypeScript:** 0 erros | **Testes:** 108/108 passando, 12 suites

— Quinn, guardiao da qualidade
