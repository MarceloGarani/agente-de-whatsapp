# Story 3.2: Google Meet Links & Meeting Invites

## Status

Done

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["typecheck", "test:unit"]

## Story

**As a** user,
**I want** to create meetings with automatic Google Meet links and send invites to participants,
**so that** I can coordinate meetings in one message without switching between apps.

## Acceptance Criteria

1. `createEvent` com `conferenceDataVersion: 1` e `conferenceData.createRequest` gera link Google Meet automaticamente
2. `calendar.events.insert` com `sendUpdates: 'all'` envia convites nativos do Google Calendar para participantes com email
3. `email.service.ts` implementa envio de convites customizados via Nodemailer OAuth2 para participantes nao-Google (OQ-6)
4. Transport Nodemailer configurado com OAuth2 (clientId, clientSecret, refreshToken)
5. Para participantes com telefone/WhatsApp, convite enviado via `whatsapp.client.sendText` com detalhes + link Meet
6. Message router integrado: intent `criar_reuniao` → extrai entities (titulo, data, hora, participantes[]) → cria evento + Meet + convites
7. Confirmacao enviada com resumo: nome, data, link Meet, status dos convites
8. Se email de participante invalido (EC-8), informa quais convites falharam sem bloquear os demais
9. Teste unitario: mock Calendar API (evento + Meet link) + mock Nodemailer (envio email)

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [x] **Task 1: Atualizar Calendar service para Meet** (AC: 1, 2)
  - [x] Atualizar `src/services/calendar.service.ts`: adicionar parametro opcional `withMeet: boolean` e `attendees: string[]` ao createEvent
  - [x] Quando withMeet=true: adicionar `conferenceDataVersion: 1` e `conferenceData: { createRequest: { requestId: uuid, conferenceSolutionKey: { type: 'hangoutsMeet' } } }` ao events.insert
  - [x] Quando attendees fornecidos: adicionar `attendees: attendees.map(email => ({ email }))` e `sendUpdates: 'all'` ao events.insert
  - [x] Extrair meetLink de response.data.conferenceData?.entryPoints?.[0]?.uri
  - [x] Retornar CalendarEvent com meetLink preenchido
- [x] **Task 2: Email service** (AC: 3, 4)
  - [x] Criar `src/services/email.service.ts`
  - [x] Configurar transport Nodemailer com OAuth2: type 'OAuth2', user (email do usuario), clientId, clientSecret, refreshToken do env
  - [x] Implementar `sendMeetingInvite(to: string, subject: string, meetLink: string, dateTime: string): Promise<void>` que envia email HTML com detalhes da reuniao e link Meet
  - [x] Timeout de 10_000ms (TIMEOUTS.NODEMAILER)
- [x] **Task 3: Convites WhatsApp** (AC: 5)
  - [x] Implementar `sendWhatsAppInvite(phone: string, title: string, meetLink: string, dateTime: string): Promise<void>` usando whatsapp.client.sendText com mensagem formatada
- [x] **Task 4: Integrar no message router** (AC: 6, 7)
  - [x] Atualizar message-router.ts: switch case CRIAR_REUNIAO
  - [x] Handler: enviar messages.processing.meeting → resolver data/hora → createEvent com withMeet=true e attendees com emails → para cada participante com email, Calendar ja enviou convite nativo → para participantes com telefone, sendWhatsAppInvite → sendText com messages.confirmations.meetingCreated interpolado com titulo, data, meetLink, lista de convites
- [x] **Task 5: Edge case — email invalido** (AC: 8)
  - [x] Para cada participante, try/catch individual: se convite falha, adicionar a lista de falhas
  - [x] Na confirmacao, listar quais convites falharam: "Convite enviado para X. Falha ao enviar para Y (email invalido)."
- [x] **Task 6: Teste unitario** (AC: 9)
  - [x] Atualizar `tests/unit/calendar.service.test.ts`: test createEvent com conferenceData retorna meetLink
  - [x] Criar `tests/unit/email.service.test.ts`: mock Nodemailer, test envio com sucesso e falha
  - [x] Test: fluxo de reuniao com 2 participantes (1 email, 1 telefone)

## Dev Notes

### Architecture Context [Source: docs/architecture.md]

**Google Meet via Calendar (Sec 5.4):**
- conferenceDataVersion: 1 no events.insert
- conferenceData.createRequest com requestId unico e type 'hangoutsMeet'
- Meet link em response.data.conferenceData.entryPoints[0].uri
- sendUpdates: 'all' envia convites Google nativos automaticamente

**Fluxo de Reuniao (Sec 6.3):**
```
"reuniao amanha 10h com joao@email.com"
→ "Organizando sua reuniao..."
→ createEvent(titulo, start, end, conferenceData)
→ Calendar envia convites nativos (sendUpdates: 'all')
→ Para emails nao-Google: email.service.sendMeetingInvite
→ Para telefones: whatsapp.client.sendText com convite
→ Confirmacao com resumo + link Meet
```

**Email Service (Sec 5.5):**
- Nodemailer com OAuth2 transport
- Calendar sendUpdates ja envia convites Google nativos (OQ-6)
- Email service para convites customizados a nao-Google users

**Messages pt-BR (Sec 14.2):**
- `messages.processing.meeting`: "Organizando sua reuniao..."
- `messages.confirmations.meetingCreated`: "Reuniao \"{titulo}\" criada para {data} as {hora}.\nLink Meet: {meetLink}\nConvites enviados: {convites}"
- `messages.errors.invalidEmail`: "O email \"{email}\" parece invalido..."

**UUID para requestId:** Usar `crypto.randomUUID()` (Node.js nativo) para gerar requestId do conferenceData.

**Project Structure:**
```
src/services/calendar.service.ts    # ATUALIZAR (Meet + attendees)
src/services/email.service.ts       # CRIAR
tests/unit/email.service.test.ts    # CRIAR
```

**Dependencia de Stories 2.1, 3.1, 1.2:** Requer google-auth, calendar.service base, whatsapp.client.

### Testing

**Framework:** Jest + ts-jest
**Mock:** googleapis calendar, nodemailer createTransport
**Casos:** Criar evento com Meet, enviar email, convite WhatsApp, email invalido

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-16 | 0.1 | Story criada a partir do PRD Epic 3 + Architecture | @sm (River) |
| 2026-02-16 | 0.2 | Validacao GO (8/10) — Status Draft → Ready | @po (Pax) |
| 2026-02-16 | 0.3 | Implementacao completa — Status Ready → InReview. 6 tasks, 6 testes novos, 0 erros TS | @dev (Dex) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References
- Nenhum issue — typecheck e testes passaram na primeira execucao

### Completion Notes List
- `src/services/calendar.service.ts` — Atualizado: createEvent recebe options `{ withMeet, attendees }`, conferenceDataVersion=1, conferenceData.createRequest com crypto.randomUUID(), sendUpdates='all', meetLink extraido de response
- `src/services/email.service.ts` — Novo: sendMeetingInvite via Nodemailer OAuth2 (Gmail), envia HTML com titulo, data, link Meet
- `src/services/message-router.ts` — Atualizado: handleCreateMeeting() integrado — processing msg → resolveDateTime → createEvent com withMeet=true → WhatsApp invites (telefone) → email invites → confirmacao com resumo
- `src/types/index.ts` — Adicionado GOOGLE_USER_EMAIL ao EnvConfig
- `src/config/env.ts` — Adicionado GOOGLE_USER_EMAIL com default ''
- Edge case: try/catch individual por participante — falhas listadas na confirmacao com "(falha)"
- WhatsApp invite inline no handleCreateMeeting (sendText com mensagem formatada)
- CRIAR_REUNIAO removido do switch "em desenvolvimento"
- Dependencia: nodemailer + @types/nodemailer instalados
- TypeScript: zero erros, 69/69 testes passando (9 suites)

### File List
- `src/services/calendar.service.ts` (MODIFIED — withMeet + attendees + conferenceData)
- `src/services/email.service.ts` (NEW — Nodemailer OAuth2 sendMeetingInvite)
- `src/services/message-router.ts` (MODIFIED — handleCreateMeeting integration)
- `src/types/index.ts` (MODIFIED — GOOGLE_USER_EMAIL in EnvConfig)
- `src/config/env.ts` (MODIFIED — GOOGLE_USER_EMAIL default)
- `tests/unit/calendar.service.test.ts` (MODIFIED — 2 testes novos: Meet link + attendees)
- `tests/unit/email.service.test.ts` (NEW — 2 testes: envio sucesso + falha)
- `tests/unit/message-router.test.ts` (MODIFIED — email.service mock + 2 testes reuniao)
- `tests/integration/webhook.test.ts` (MODIFIED — email.service mock)

## QA Results

**Verdict: PASS**

| Check | Result | Notes |
|-------|--------|-------|
| Code review | OK | Meet links + email invites bem integrados |
| Unit tests | OK | Cobertura adequada (email service + router) |
| Acceptance criteria | OK | Todos os ACs atendidos |
| No regressions | OK | Nenhuma regressao |
| Performance | OK | N/A |
| Security | OK | Sem vulnerabilidades detectadas |
| Documentation | OK | Dev Notes completas |

**TypeScript:** 0 erros | **Testes:** 108/108 passando, 12 suites

— Quinn, guardiao da qualidade
