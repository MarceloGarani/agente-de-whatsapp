# Story 4.2: Automatic Reminders Scheduler

## Status

Done

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["typecheck", "test:unit"]

## Story

**As a** user,
**I want** to receive automatic WhatsApp reminders before my upcoming appointments,
**so that** I never miss an important event or meeting.

## Acceptance Criteria

1. `reminder.scheduler.ts` usa node-cron para executar a cada 5 minutos
2. Consulta Calendar API (`listUpcoming`) para eventos nos proximos 30 minutos
3. `Set<string>` em memoria com eventIds ja lembrados para evitar duplicatas
4. Lembrete enviado via `whatsapp.client.sendText` com: nome do evento, horario e link Meet (se aplicavel)
5. Formato da mensagem: "Lembrete: '[titulo]' comeca em [X] minutos. [Link Meet se houver]"
6. Se servidor reiniciar, Set limpa — pior caso e lembrete duplicado (aceitavel v1)
7. Scheduler iniciado no startup do server (`src/server.ts`)
8. Se Calendar API indisponivel durante verificacao, loga erro e tenta novamente no proximo ciclo
9. Teste unitario com mock: Calendar retorna eventos proximos → lembrete enviado → eventId adicionado ao Set → segundo ciclo nao reenvia

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [x] **Task 1: Reminder scheduler service** (AC: 1, 2, 3, 4, 5, 6)
  - [x] Criar `src/services/reminder.scheduler.ts`
  - [x] Importar node-cron, calendar.service (listUpcoming), whatsapp.client (sendText)
  - [x] Criar `sentReminders: Set<string>` para rastrear eventIds ja lembrados
  - [x] Implementar `checkAndSendReminders(): Promise<void>` que:
    1. Calcula timeMin = now, timeMax = now + 30min (ambos em ISO 8601)
    2. Chama listUpcoming(timeMin, timeMax)
    3. Para cada evento: se eventId NAO esta no sentReminders Set → enviar lembrete via sendText → adicionar eventId ao Set
  - [x] Formatar mensagem: messages.confirmations.reminder interpolado com titulo, minutos ate evento, meetLink (se existir)
  - [x] Implementar `startScheduler(): void` que registra cron job '*/5 * * * *' (a cada 5 min) chamando checkAndSendReminders
  - [x] Exportar startScheduler para uso no server.ts
- [x] **Task 2: Integrar no server.ts** (AC: 7)
  - [x] Atualizar `src/server.ts`: chamar startScheduler() apos server.listen()
  - [x] Log: "Reminder scheduler started (every 5 minutes)"
- [x] **Task 3: Error handling** (AC: 8)
  - [x] try/catch em checkAndSendReminders: se Calendar API falha, logar erro com service='reminder', action='check' e retornar silenciosamente (proximo ciclo tentara novamente)
  - [x] Se sendText falha para um lembrete, logar erro mas continuar com os demais
- [x] **Task 4: Teste unitario** (AC: 9)
  - [x] Criar `tests/unit/reminder.scheduler.test.ts`
  - [x] Mock de calendar.service.listUpcoming e whatsapp.client.sendText
  - [x] Test: listUpcoming retorna 2 eventos → 2 lembretes enviados → 2 eventIds no Set
  - [x] Test: segundo ciclo com mesmos eventos → 0 lembretes enviados (ja no Set)
  - [x] Test: Calendar API falha → nenhum lembrete enviado, sem crash
  - [x] Test: evento com meetLink → mensagem inclui link

## Dev Notes

### Architecture Context [Source: docs/architecture.md]

**Reminder Scheduler (Sec 6.4):**
```
node-cron (5min) → listUpcoming(now, now+30min)
→ para cada evento nao lembrado
  → sendText(lembrete + meetLink)
  → marcar eventId como enviado
```

**Data Model ReminderState (Sec 4.2):**
```typescript
interface ReminderState {
  sentEventIds: Set<string>;
  lastCheckAt: Date;
}
```

**Cron Pattern:** `*/5 * * * *` — a cada 5 minutos

**Messages pt-BR (Sec 14.2):**
- `messages.confirmations.reminder`: "Lembrete: \"{titulo}\" comeca em {minutos} minutos.{meetLink}"
  - Se meetLink: "\nLink Meet: {meetLink}"
  - Se sem meetLink: string vazia

**Memory Management (Sec 11.6):**
- Set<eventId> cresce ~30 entries/dia
- Se servidor reiniciar, Set limpa → pior caso: lembrete duplicado (aceitavel v1)
- Considerar limpeza diaria do Set (remover eventIds de eventos ja passados)

**node-cron Pattern:**
```typescript
import cron from 'node-cron';

export function startScheduler(): void {
  cron.schedule('*/5 * * * *', async () => {
    try {
      await checkAndSendReminders();
    } catch (error) {
      logger.error({ service: 'reminder', action: 'check', error });
    }
  });
  logger.info({ service: 'reminder', action: 'start', message: 'Scheduler started (every 5 minutes)' });
}
```

**WhatsApp Phone Number:** O scheduler envia lembretes para o usuario principal. Na v1 single-user, o phone number e fixo (env.WHATSAPP_USER_PHONE ou hardcoded). **Nota:** Pode ser necessario adicionar uma variavel WHATSAPP_USER_PHONE ao .env para identificar o numero do usuario.

**Project Structure:**
```
src/services/reminder.scheduler.ts       # CRIAR
tests/unit/reminder.scheduler.test.ts    # CRIAR
src/server.ts                            # ATUALIZAR (chamar startScheduler)
```

**Dependencia de Stories 3.1, 1.2:** Requer calendar.service (listUpcoming), whatsapp.client (sendText).

### Testing

**Framework:** Jest + ts-jest
**Mock:** calendar.service.listUpcoming, whatsapp.client.sendText, node-cron (nao testar o scheduling, testar checkAndSendReminders diretamente)
**Casos:** Lembretes enviados, duplicatas evitadas, Calendar falha, evento com/sem Meet

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-16 | 0.1 | Story criada a partir do PRD Epic 4 + Architecture | @sm (River) |
| 2026-02-16 | 0.2 | Validacao GO (8/10) — Status Draft → Ready | @po (Pax) |
| 2026-02-16 | 0.3 | Implementacao completa — Status Ready → InReview | @dev (Dex) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References
- Nenhum erro encontrado. Implementacao passou no typecheck e testes na primeira execucao.

### Completion Notes List
- `reminder.scheduler.ts`: node-cron */5 * * * * → listUpcoming(now, now+30min) → sendText lembretes
- `sentReminders` Set<string> para evitar duplicatas; limpa no restart (aceitavel v1)
- WHATSAPP_USER_PHONE adicionado ao EnvConfig + env.ts (variavel opcional)
- Guard: se WHATSAPP_USER_PHONE nao configurado, loga warn e retorna silenciosamente
- try/catch individual por lembrete: se sendText falha para um, continua com os demais
- server.ts: startScheduler() chamado apos server.listen()
- 6 testes unitarios: envio OK, deduplicacao, Calendar fail, com/sem Meet link, sendText parcial fail
- Mock de reminder.scheduler adicionado ao teste de integracao
- 91 testes passando, 11 suites, 0 erros TS

### File List
- `src/services/reminder.scheduler.ts` (CRIADO)
- `src/server.ts` (ATUALIZADO — import + startScheduler)
- `src/types/index.ts` (ATUALIZADO — WHATSAPP_USER_PHONE)
- `src/config/env.ts` (ATUALIZADO — WHATSAPP_USER_PHONE)
- `tests/unit/reminder.scheduler.test.ts` (CRIADO)
- `tests/integration/webhook.test.ts` (ATUALIZADO — reminder.scheduler mock)

## QA Results

**Verdict: PASS**

| Check | Result | Notes |
|-------|--------|-------|
| Code review | OK | Scheduler com cron + deduplicacao por Set |
| Unit tests | OK | 6 testes cobrindo envio, dedup, falhas parciais |
| Acceptance criteria | OK | Todos os ACs atendidos |
| No regressions | OK | Nenhuma regressao |
| Performance | OK | Cron */5 adequado para lembretes |
| Security | OK | Sem vulnerabilidades detectadas |
| Documentation | OK | Dev Notes completas |

**TypeScript:** 0 erros | **Testes:** 108/108 passando, 12 suites

— Quinn, guardiao da qualidade
