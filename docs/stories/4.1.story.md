# Story 4.1: Audio Transcription (ffmpeg + Whisper)

## Status

Done

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["typecheck", "test:unit"]

## Story

**As a** user,
**I want** to send voice messages on WhatsApp and have the assistant understand them,
**so that** I can interact hands-free without typing.

## Acceptance Criteria

1. `audio-transcriber.ts` implementa pipeline: Buffer OGG/Opus → temp file → fluent-ffmpeg → MP3 → Whisper API → texto
2. Conversao via fluent-ffmpeg com codec libmp3lame, removendo temp files apos processamento
3. Whisper API chamada com model `gpt-4o-mini-transcribe` e `language: 'pt'`
4. Timeout de 15s para chamada ao Whisper API
5. Message router atualizado: tipo `audio` → download media → audio-transcriber → texto → intent-classifier → resposta
6. Se transcricao falha ou audio inaudivel (EC-3), responde: "Nao consegui entender o audio. Pode enviar por texto?"
7. Temp files sempre removidos (try/finally) para evitar acumulo em disco
8. Teste unitario com mock: ffmpeg conversao + Whisper transcricao + fluxo de erro

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [x] **Task 1: Audio transcriber service** (AC: 1, 2, 3, 4, 7)
  - [x] Criar `src/services/audio-transcriber.ts`
  - [x] Implementar `transcribeAudio(buffer: Buffer): Promise<string>` com pipeline:
    1. Salvar buffer em temp file com extensao .ogg (`os.tmpdir() + '/audio-${Date.now()}.ogg'`)
    2. Converter OGG → MP3 via fluent-ffmpeg: `ffmpeg(inputPath).audioCodec('libmp3lame').format('mp3').save(outputPath)` — wrappear em Promise
    3. Ler MP3 como File para Whisper API
    4. Chamar `openai.audio.transcriptions.create({ model: 'gpt-4o-mini-transcribe', file: mp3File, language: 'pt' })` com timeout de 15_000ms (TIMEOUTS.OPENAI_WHISPER)
    5. Retornar response.text
  - [x] Usar try/finally para SEMPRE remover temp files (.ogg e .mp3) com fs.unlink
  - [x] Usar handleApiError para erros, logando service='audio-transcriber'
- [x] **Task 2: Integrar no message router** (AC: 5, 6)
  - [x] Atualizar `src/services/message-router.ts`: case AUDIO
  - [x] Handler: enviar messages.processing.audio → downloadMedia(audioId) → transcribeAudio(buffer) → classifyIntent(texto) → continuar como texto
  - [x] Se transcribeAudio falha ou retorna string vazia: sendText com messages.errors.whisperFailed
- [x] **Task 3: Teste unitario** (AC: 8)
  - [x] Criar `tests/unit/audio-transcriber.test.ts`
  - [x] Mock de fluent-ffmpeg (simular conversao bem-sucedida)
  - [x] Mock de openai.audio.transcriptions.create
  - [x] Test: buffer OGG → MP3 convertido → Whisper retorna texto → texto retornado
  - [x] Test: Whisper falha → AppError com messages.errors.whisperFailed
  - [x] Test: ffmpeg falha → AppError
  - [x] Test: temp files sao removidos mesmo em caso de erro (verificar fs.unlink chamado)

## Dev Notes

### Architecture Context [Source: docs/architecture.md]

**OpenAI Whisper (Sec 5.7):**
- Model: gpt-4o-mini-transcribe
- Language: 'pt'
- Input: MP3 (convertido de OGG/Opus via ffmpeg)
- Timeout: 15s (TIMEOUTS.OPENAI_WHISPER)
- Custo: ~$4.50/mes

**Fluxo de Audio (Sec 6.2):**
```
Audio recebido → downloadMedia(audioId) → Buffer OGG/Opus
→ ffmpeg: OGG → MP3 (temp files)
→ Whisper API: MP3 → texto pt-BR
→ classifyIntent(texto) → continua como fluxo de texto
```

**ffmpeg Pattern:**
```typescript
import ffmpeg from 'fluent-ffmpeg';
import { createReadStream } from 'fs';

function convertOggToMp3(inputPath: string, outputPath: string): Promise<void> {
  return new Promise((resolve, reject) => {
    ffmpeg(inputPath)
      .audioCodec('libmp3lame')
      .format('mp3')
      .on('end', resolve)
      .on('error', reject)
      .save(outputPath);
  });
}
```

**Cleanup Pattern:**
```typescript
const tmpOgg = path.join(os.tmpdir(), `audio-${Date.now()}.ogg`);
const tmpMp3 = path.join(os.tmpdir(), `audio-${Date.now()}.mp3`);
try {
  await fs.writeFile(tmpOgg, buffer);
  await convertOggToMp3(tmpOgg, tmpMp3);
  const transcription = await whisper(tmpMp3);
  return transcription;
} finally {
  await fs.unlink(tmpOgg).catch(() => {});
  await fs.unlink(tmpMp3).catch(() => {});
}
```

**Messages pt-BR (Sec 14.2):**
- `messages.processing.audio`: "Processando seu audio..."
- `messages.errors.whisperFailed`: "Nao consegui entender o audio. Pode enviar por texto?"

**System Dependency:** ffmpeg deve estar instalado (brew install ffmpeg / apt-get install ffmpeg). Em Railway, Nixpacks detecta fluent-ffmpeg e instala ffmpeg automaticamente.

**Project Structure:**
```
src/services/audio-transcriber.ts          # CRIAR
tests/unit/audio-transcriber.test.ts       # CRIAR
src/services/message-router.ts             # ATUALIZAR (case AUDIO)
```

**Dependencia de Stories 1.2, 1.3, 1.4:** Requer whatsapp.client (downloadMedia), intent-classifier, message-router.

### Testing

**Framework:** Jest + ts-jest
**Mock:** jest.mock('fluent-ffmpeg'), jest.mock('openai')
**Nota:** Para testar ffmpeg, mock do modulo inteiro retornando promise resolve. Para temp files, usar jest.spyOn(fs, 'unlink').
**Casos:** Transcricao OK, Whisper falha, ffmpeg falha, cleanup de temp files

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-16 | 0.1 | Story criada a partir do PRD Epic 4 + Architecture | @sm (River) |
| 2026-02-16 | 0.2 | Validacao GO (8/10) — Status Draft → Ready | @po (Pax) |
| 2026-02-16 | 0.3 | Implementacao completa — Status InProgress → InReview | @dev (Dex) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References
- TS2352 em audio-transcriber.test.ts: cast `fn as Record<string, unknown>` → `fn as unknown as Record<string, unknown>`
- createReadStream mock: `jest.spyOn` falhou (propriedade nao-configuravel), resolvido com `jest.mock('node:fs')` parcial
- message-router.test.ts: `jest.clearAllMocks()` nao reseta implementacoes; `mockDownloadMedia.mockRejectedValue` de teste anterior persistia. Corrigido com `mockDownloadMedia.mockResolvedValue()` explicito nos testes de audio

### Completion Notes List
- `audio-transcriber.ts`: Pipeline completo OGG→MP3→Whisper com try/finally cleanup
- `message-router.ts`: handleAudioMessage integrado — download→transcribe→classifyIntent→route
- 4 testes unitarios do audio-transcriber (pipeline OK, Whisper fail, ffmpeg fail, cleanup)
- 3 testes de audio routing no message-router (transcribe+route, empty text, error)
- Mock do audio-transcriber adicionado ao teste de integracao
- 85 testes passando, 10 suites, 0 erros TS

### File List
- `src/services/audio-transcriber.ts` (CRIADO)
- `src/services/message-router.ts` (ATUALIZADO — handleAudioMessage)
- `tests/unit/audio-transcriber.test.ts` (CRIADO)
- `tests/unit/message-router.test.ts` (ATUALIZADO — audio mocks + 3 testes)
- `tests/integration/webhook.test.ts` (ATUALIZADO — audio-transcriber mock)

## QA Results

**Verdict: PASS**

| Check | Result | Notes |
|-------|--------|-------|
| Code review | OK | Pipeline ffmpeg + Whisper robusto com cleanup |
| Unit tests | OK | Cobertura adequada com mocks de node:fs e fluent-ffmpeg |
| Acceptance criteria | OK | Todos os ACs atendidos |
| No regressions | OK | Nenhuma regressao |
| Performance | OK | N/A |
| Security | OK | Temp files limpos em finally block |
| Documentation | OK | Dev Notes completas |

**TypeScript:** 0 erros | **Testes:** 108/108 passando, 12 suites

— Quinn, guardiao da qualidade
