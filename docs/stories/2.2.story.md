# Story 2.2: Google Drive Upload & Auto-Organization

## Status

Done

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["typecheck", "test:unit"]

## Story

**As a** user,
**I want** my files to be automatically organized in Google Drive by type and date,
**so that** I can find them easily later without manual sorting.

## Acceptance Criteria

1. `drive.service.ts` implementa `uploadFile(buffer, filename, mimeType)` usando `drive.files.create` com media stream
2. Organizacao automatica: pasta raiz por tipo (`documentos/`, `imagens/`, `planilhas/`, `outros/`) baseada em mime_type
3. Subpasta por mes/ano (ex: `documentos/02-2026/`) criada automaticamente se nao existir
4. Retorna objeto `{ fileId, webViewLink, folderPath }` apos upload bem-sucedido
5. Scope utilizado: `drive.file` (acesso apenas a arquivos criados pelo app)
6. Timeout de 10s para chamadas a Drive API
7. Erros de upload (quota, permissao, rede) logados e notificados ao usuario (NFR-3)
8. Teste unitario com mock da googleapis: criacao de pasta + upload + retorno de link

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [x] **Task 1: Drive service** (AC: 1, 2, 3, 4, 5, 6)
  - [x] Criar `src/services/drive.service.ts`
  - [x] Importar googleapis e getAuthenticatedClient
  - [x] Implementar funcao auxiliar `getMimeCategory(mimeType: string): string` que mapeia: application/pdf|msword|document → 'documentos', image/* → 'imagens', application/spreadsheet|excel|csv → 'planilhas', default → 'outros'
  - [x] Implementar funcao auxiliar `getOrCreateFolder(drive, name: string, parentId?: string): Promise<string>` que: busca pasta existente via drive.files.list com query name e mimeType folder, se nao existe cria via drive.files.create, retorna folderId
  - [x] Implementar funcao auxiliar `getMonthYearFolder(drive, parentFolderId: string): Promise<string>` que cria/obtém subpasta no formato "MM-YYYY" (ex: "02-2026")
  - [x] Implementar `uploadFile(buffer: Buffer, filename: string, mimeType: string): Promise<DriveUpload>` que: determina categoria, cria/obtem pasta de categoria, cria/obtem subpasta mes-ano, faz upload via drive.files.create com media body Buffer e parents [subpastaId], retorna { fileId, webViewLink, folderPath: `${categoria}/${mesAno}/` }
  - [x] Timeout de 10_000ms (TIMEOUTS.GOOGLE_APIS) em chamadas
- [x] **Task 2: Error handling** (AC: 7)
  - [x] Usar handleApiError para erros de Drive API
  - [x] Mensagem: messages.errors.driveUnavailable
  - [x] Logar com service='drive', action='upload'
- [x] **Task 3: Teste unitario** (AC: 8)
  - [x] Criar `tests/unit/drive.service.test.ts`
  - [x] Mock de googleapis (drive.files.list, drive.files.create)
  - [x] Test: uploadFile com PDF → pasta 'documentos/02-2026/', retorna fileId e webViewLink
  - [x] Test: uploadFile com imagem → pasta 'imagens/02-2026/'
  - [x] Test: pasta ja existente → nao cria duplicata
  - [x] Test: erro de API → AppError com mensagem pt-BR

## Dev Notes

### Architecture Context [Source: docs/architecture.md]

**Google Drive API v3 (Sec 5.3):**
- Upload: drive.files.create com media stream
- Folder check: drive.files.list com query `name='X' and mimeType='application/vnd.google-apps.folder'`
- Scope: drive.file (apenas arquivos criados pelo app)
- Timeout: 10s

**Data Model DriveUpload (Sec 4.2):**
```typescript
interface DriveUpload {
  fileId: string;
  webViewLink: string;
  folderPath: string;   // ex: "documentos/02-2026/"
  filename: string;
}
```

**Mime Type Mapping:**
- `application/pdf`, `application/msword`, `application/vnd.openxmlformats-officedocument.wordprocessingml.document` → `documentos/`
- `image/*` → `imagens/`
- `application/vnd.ms-excel`, `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`, `text/csv` → `planilhas/`
- Qualquer outro → `outros/`

**Project Structure:**
```
src/services/drive.service.ts          # CRIAR
tests/unit/drive.service.test.ts       # CRIAR
```

**Dependencia de Story 2.1:** Requer google-auth.ts (getAuthenticatedClient).

### Testing

**Framework:** Jest + ts-jest
**Mock:** jest.mock('googleapis') — mock de drive.files.list e drive.files.create
**Casos:** Upload por tipo MIME, pasta existente vs nova, erros de API

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-16 | 0.1 | Story criada a partir do PRD Epic 2 + Architecture | @sm (River) |
| 2026-02-16 | 0.2 | Validacao GO (8/10) — Status Draft → Ready | @po (Pax) |
| 2026-02-16 | 0.3 | Implementacao completa — Status Ready → InReview. 3 tasks, 9 testes, 0 erros TS | @dev (Dex) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References
- Nenhum issue encontrado — typecheck e testes passaram na primeira execucao

### Completion Notes List
- `src/services/drive.service.ts` — uploadFile() com auto-organizacao por MIME type (documentos/imagens/planilhas/outros) + subpasta MM-YYYY
- getMimeCategory() mapeia MIME → categoria de pasta
- getOrCreateFolder() busca/cria pasta sem duplicatas via drive.files.list + drive.files.create
- Upload via Readable stream do Buffer
- Retorna DriveUpload { fileId, webViewLink, folderPath, filename }
- 9 testes unitarios (5 getMimeCategory + 4 uploadFile incluindo reuso de pasta e erro)
- TypeScript: zero erros, 46/46 testes passando (7 suites)

### File List
- `src/services/drive.service.ts` (CREATED)
- `tests/unit/drive.service.test.ts` (CREATED)

## QA Results

**Verdict: PASS**

| Check | Result | Notes |
|-------|--------|-------|
| Code review | OK | Drive service com organizacao por tipo/mes |
| Unit tests | OK | Cobertura adequada |
| Acceptance criteria | OK | Todos os ACs atendidos |
| No regressions | OK | Nenhuma regressao |
| Performance | OK | N/A |
| Security | OK | Sem vulnerabilidades detectadas |
| Documentation | OK | Dev Notes completas |

**TypeScript:** 0 erros | **Testes:** 108/108 passando, 12 suites

— Quinn, guardiao da qualidade
